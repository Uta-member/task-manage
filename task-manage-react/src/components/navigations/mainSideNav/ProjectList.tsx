import { List, ListItem } from "@mui/joy";

const ProjectList = () => {
  return (
    <List sx={{ width: "100%", flexGrow: "unset", alignItems: "start" }}>
      <ListItem>プロジェクト</ListItem>
    </List>
  );
};

export default ProjectList;
